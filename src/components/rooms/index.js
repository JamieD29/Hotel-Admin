import RoomCreate from './CreateRoom';
import RoomList from './ListRoom';
import RoomEdit from './EditRoom';
import ShowRoom from './ShowRoom';

const resource = {
  list: RoomList,
  create: RoomCreate,
  edit: RoomEdit,
  show: ShowRoom,
};

export default resource;
